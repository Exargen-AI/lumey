{
  "name": "@exargen/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "The Lumey Platform SDK — the typed client agents and integrations use to talk to Lumey.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "gen:python": "tsx scripts/gen-python.cli.ts"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.9"
  }
}
